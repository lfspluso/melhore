package com.melhoreapp.feature.reminders.ui.list

/**
 * Tab for the Melhores (reminder list) screen: Tarefas (tasks/regular reminders)
 * or Rotinas (routine reminders).
 */
enum class ReminderTab {
    TAREFAS,
    ROTINAS
}
